VITE_API_BASE_URL=/api/v1

# Google OAuth Client ID (Google Cloud Console > APIs & Services > Credentials).
# Must match GOOGLE_CLIENT_ID on the server - required for "Continue with Google" to work.
VITE_GOOGLE_CLIENT_ID=
